export default class LoaderSignal
{
    
	// 加载配置	
	static onLoadedConfig(index: number, count: number)
	{
		// this.setTip(LoadingStateTxt.LoadedConfig);
		// this.loading(40 + Math.floor((index / count) * 20));
	}
}